<Window x:Class="Приложение_Турагенства.UI.Wnd.wndCaptcha"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Приложение_Турагенства.UI.Wnd"
        xmlns:cmcl="clr-namespace:CustomControls"
        mc:Ignorable="d"
        Title="Путешествия по России: Провверка" Height="400" Width="600" Loaded="Window_Loaded"
        ResizeMode="CanMinimize" 
        WindowStartupLocation="CenterScreen" 
        Background="#bae3e8" 
        Icon="/Приложение_Турагенства;component/Resource/Logo.png">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Grid.ColumnSpan="2" Width="300" Height="100">
            <Image x:Name="imgCaptcha" Width="300" Height="100"/>
        </Border>
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="Введите Captcha:"/>
            <cmcl:clPlaceholderTextBox x:Name="txbxCaptchaText" Width="250" Height="30" Placeholder="Введите текст с изображения..." Radius="10" Margin="10 8 0 8" MaxLength="5" TextChanged="txbxCaptchaText_TextChanged"/>
        </StackPanel>
        <Button x:Name="btnCreateCaptcha" Grid.Row="2" Grid.Column="0" Width="130" Height="50" Content="Обновить картинку" Style="{StaticResource ButtonBasicNoSize}" Click="btnCreateCaptcha_Click" MouseLeave="Button_MouseLeave" MouseEnter="Button_MouseEnter"/>
        <Button x:Name="btnCheckCaptcha" Grid.Row="2" Grid.Column="1" Width="130" Height="50" Content="Проерить Captcha" IsEnabled="False" Style="{StaticResource ButtonNoSizeIsNotEnabled}" Click="btnCheckCaptcha_Click" MouseLeave="Button_MouseLeave" MouseEnter="Button_MouseEnter"/>
    </Grid>

    <Window.Resources>
        <Style TargetType="Border">
            <Style.Setters>
                <Setter Property="BorderBrush" Value="#445c93"/>
                <Setter Property="BorderThickness" Value="1.5"/>
            </Style.Setters>
        </Style>
        <Style TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontFamily" Value="Comic Sans MS"/>
            </Style.Setters>
        </Style>
    </Window.Resources>
</Window>
